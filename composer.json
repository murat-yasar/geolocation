{
   "name": "murat-yasar/geolocation",
   "description": "A simple PHP project to be deployed on Docker.",
   "require": {
       "php": "^7.4 || ^8.0"
   },
   "scripts": {
       "start": "php -S 0.0.0.0:${PORT} -t public"
   },
   "config": {
       "platform-check": false
   }
}
